---
description: This rule is helpful when add or updating dependencies, or when running `uv`.
globs: pyproject.toml
---
- When adding new dependencies, DO NOT directly edit `pyproject.toml`. Instead use `uv add ...` to add the new dependency.
- When running scripts or running the application, use `uv run ...`. Here is an example:

```
uv run scripts/generate_metadata.p
y --file app/content/notes/2023-09-06-Notes:-Postgres-Database-Scaling-with-Liqu
ibase-on-Kubernetes.md                        python scripts/generate_metadata.py --file app/content/notes/2023-09-06-Notes:-Postgres-Database-Scaling-with-Liqu
<caling-with-Liquibase-on-Kubernetes.md
`